Ok, so right now, nothing is happening much, but that'll change soon.

So running './run2simulators.sh' does exactly what you'd expect, and running 'python example1.py' starts the simple demonstration of how we're running 2 TUIO trackers on 2 different ports, but listening to them with one client.

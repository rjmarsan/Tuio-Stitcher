Ok, so right now, EPIC is happening much, but that'll GET MORE EPIC soon.

So running './run2simulators.sh' does exactly what you'd expect, as does './runreader.sh', and running 'python basicstitch.py' starts the simple demonstration of how we're running 2 TUIO trackers on 2 different ports, but listening to them with one client.
